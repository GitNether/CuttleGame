# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Expo
.expo/
.expo-shared/
expo-env.d.ts
web-build/

# EAS / native builds (generated by `expo prebuild` — we stay managed)
apps/mobile/ios/
apps/mobile/android/
*.ipa
*.aab
*.apk

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log

# Local env / secrets — never commit Firebase keys for other projects,
# google-services files are per-developer
.env
.env.*
google-services.json
GoogleService-Info.plist

# Test / coverage
coverage/

# Editor & OS
.DS_Store
.idea/
.vscode/
*.log
