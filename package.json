{
  "name": "cuttlegame",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspace @cuttle/engine",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
