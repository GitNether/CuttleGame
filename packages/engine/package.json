{
  "name": "@cuttle/engine",
  "version": "1.0.0",
  "description": "Pure TypeScript engine for the Cuttle card game — framework-agnostic, unit-tested.",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vitest": "^3.2.4"
  }
}
